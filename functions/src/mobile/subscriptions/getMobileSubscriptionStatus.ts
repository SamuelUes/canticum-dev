import * as functions from 'firebase-functions';
import { notImplemented } from '../../shared/errors';

export const getMobileSubscriptionStatus = functions.https.onCall(async () => {
  notImplemented('getMobileSubscriptionStatus');
});
