import * as functions from 'firebase-functions';
import { notImplemented } from '../../shared/errors';

export const syncPremiumStatus = functions.https.onCall(async () => {
  notImplemented('syncPremiumStatus');
});
