import * as functions from 'firebase-functions';
import { notImplemented } from '../../shared/errors';

export const validateMobilePurchase = functions.https.onCall(async () => {
  notImplemented('validateMobilePurchase');
});
