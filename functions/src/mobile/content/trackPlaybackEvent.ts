import * as functions from 'firebase-functions';
import { notImplemented } from '../../shared/errors';

export const trackPlaybackEvent = functions.https.onCall(async () => {
  notImplemented('trackPlaybackEvent');
});
